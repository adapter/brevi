{
  "name": "@brevi/docs",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run scripts/build-changelog.ts && bun run scripts/copy-payload.ts && astro dev",
    "start": "bun run scripts/build-changelog.ts && bun run scripts/copy-payload.ts && astro dev",
    "build": "bun run scripts/build-changelog.ts && bun run scripts/copy-payload.ts && astro build",
    "lint": "oxlint --max-warnings 0",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "wrangler deploy --env production",
    "deploy:preview": "wrangler versions upload --env production"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.2.2",
    "posthog-js": "^1.417.4",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "oxlint": "^1.78.0",
    "wrangler": "^4.123.0"
  }
}
