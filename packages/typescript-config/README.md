# @repo/typescript-config

Internal (unpublished) shared TypeScript configuration for the brevi workspace.

`base.json` is the strict NodeNext baseline every package extends; browser and worker packages override `lib`/`types` locally (see `apps/app` and `apps/api`).
